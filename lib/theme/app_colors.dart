import 'package:flutter/material.dart';

class AppColors {
  // Fondos
  static const background = Color(0xFFF5F7FB); // --bg
  static const card = Color(0xFFFFFFFF);       // --card

  // Texto
  static const textPrimary = Color(0xFF0E1116); // --text
  static const textMuted = Color(0xFF5B6372);   // --muted

  // Marca
  static const primary = Color(0xFFFF5A36);     // --accent
  static const primaryVariant = Color(0xFFFF8A65); // --accent-2

  // Bordes
  static const border = Color(0xFFE6EAF2);      // --border

  // Estados
  static const success = Color(0xFF16A34A);     // --ok
  static const error = Color(0xFFEF4444);       // --ko

  // Sombra aproximada y focus-ring (opcionales)
  static const shadowColor = Color(0x14000000); // rgba(0,0,0,0.08 aprox.)
  static const focusRing = Color(0x40FF5A36);   // rgba(255,90,54,0.25)
}
